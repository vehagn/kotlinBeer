datasource-url: jdbc:h2:mem:db/test;DB_CLOSE_ON_EXIT=FALSE
datasource-username: sa
datasource-password: sa

azure:
  keyvault:
    enabled: false

spring:
  flyway:
    enabled: false #We perform a manual migration in the integration tests
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false